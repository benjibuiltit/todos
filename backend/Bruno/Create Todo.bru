meta {
  name: Create Todo
  type: http
  seq: 3
}

post {
  url: {{base_url}}/todos
  body: json
  auth: inherit
}

body:json {
  {
    "todo": {
       "title": "My Todo",
      "description": "A little more about what needs doing.",
      "dueDate": "2024-10-15T16:13:36.294Z"
    }
  }
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("todo_id", data.id);
}
